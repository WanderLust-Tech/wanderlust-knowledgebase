# GitHub Actions Environment Variables
# This file serves as documentation for required GitHub Secrets
# and provides fallback values for development

# Required GitHub Secrets for production builds:
# VITE_API_URL - Production API URL (e.g., https://api.wander-lust.tech)
# VITE_APP_TITLE - Application title
# 
# The VITE_APP_VERSION is automatically set from package.json version during build

# Development fallbacks (used when secrets are not available)
VITE_API_URL=http://localhost:5000
VITE_APP_TITLE=Wanderlust Knowledge Base (Dev)
VITE_APP_VERSION=dev
